import XCTest
@testable import OYCountDownManager

final class OYCountDownManagerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(OYCountDownManager().text, "Hello, World!")
    }
}
